Options -Indexes
RewriteEngine On

# Prevent direct access to files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Only route /api/* to index.php
RewriteCond %{REQUEST_URI} ^/api/ [NC]
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
